version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    preBuild: 
      commands:
        - yarn install
    build:
      commands: 
        - yarn build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: []
